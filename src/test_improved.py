"""
Compatibility wrapper: tests have been moved to `tests/`.
Run tests from repository root with pytest, or execute the test files under `tests/` directly.

This file remains for backward compatibility and will print guidance.
"""

if __name__ == "__main__":
    print("This test was moved to 'tests/test_improved.py'.")
    print("Run: python -m pytest tests/test_improved.py -q")
